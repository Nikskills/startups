export type User = {
    email: string;
    password: string;
    name: string;
    username: string;
    id: string;
}

export type Startup = {
    id: string;
    title: string;
    slug: string;
    description: string;
    author: string;
    views: string
    category: string;
    pitch: string;
    image: string;
}